name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.7"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK
        run: |
          yes | sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Extract Technician Command Center v5
        run: |
          rm -rf project
          mkdir -p project
          unzip -q USBPhoneInfo_v5_Technician_CommandCenter.zip -d project

      - name: Find Android project
        run: |
          echo "Searching for Android project..."
          find project -maxdepth 5 \
            \( -name "settings.gradle" -o -name "settings.gradle.kts" \) \
            -print

      - name: Build Technician Command Center v5
        working-directory: project
        run: |
          if [ -f "./gradlew" ]; then
            chmod +x ./gradlew
            ./gradlew assembleDebug --no-daemon
          else
            gradle assembleDebug --no-daemon
          fi

      - name: Find APK
        run: |
          echo "Searching for APK..."
          find project -type f -name "*.apk" -print

      - name: Upload Technician Command Center v5 APK
        uses: actions/upload-artifact@v4
        with:
          name: USBPhoneInfo-Technician-CommandCenter-v5-APK
          path: project/**/build/outputs/apk/debug/*.apk
          if-no-files-found: error
